# Supabase (frontend — safe to expose with RLS)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Supabase (backend only — NEVER expose to frontend)
SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Gemini AI (backend only — NEVER expose to frontend)
GEMINI_API_KEY=

# Optional: override Gemini model (default: gemini-2.0-flash)
# GEMINI_MODEL=
